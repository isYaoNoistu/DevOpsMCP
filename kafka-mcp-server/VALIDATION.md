# 0.2.0-trial 验收记录

验收日期：2026-09-18。范围仅为 Kafka MCP；源码修改未提交，构建版本标记 dirty，具体源码清单和二进制哈希见 dist/kafka-mcp-server.build-info.json。

## 已通过

- `go test ./... -count=1 -timeout=60s`、`go vet ./...`。
- kfake 协议回归：空组历史位点、显式 Topic 白名单、缺少提交记录、分区范围上限、空配置键、部分 broker 失败及对象归属、鉴权/网络/TLS 错误脱敏、列表过滤/分页/截断/取消，以及已有的只读协议和事务隔离测试。
- 工具层：10 个只读工具；同目标最多 2 个查询，跨目标独立；取消和创建客户端失败后释放名额；非法作用域在连接前拒绝。
- Windows 新二进制 stdio 冒烟：版本信息、初始化、工具列表和本地目标查询。
- 真实测试集群只读验收：Topic/消费组发现及 Topic 翻页；6 小时配置覆盖；省略配置键与空数组均返回相同 33 项配置；空组显式 Topic 返回 3 个分区的历史位点；抽样 5 条记录仅返回元数据；专用空组提交位点在抽样前后不变；查询不存在的 Topic 返回错误且未自动创建。

## 尚未验证的生产条件

- 没有 Kafka CLI 对照结果；真实活跃消费组专项验收未执行。
- 真实 TLS/SASL、服务端 ACL 的组合验收未执行。错误分类测试不是握手或权限验收。
- 没有在真实集群上注入节点故障、网络阻断或生产负载；相关异常及并发路径使用本地测试覆盖。
- 仅完成 Windows amd64 打包和运行验收，不据此宣称其他平台已经验收。

本版本适合先按已验证的明文只读连接方式受控试用，不等于完成全部生产场景认证。保持 allow_payload=false。当前 MCP 配置未被自动修改，旧程序运行期间新包使用带版本号的路径。
