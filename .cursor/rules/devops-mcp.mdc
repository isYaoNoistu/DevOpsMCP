---
description: DevOpsMCP 只读约定：查监控/发版/数据库时不写、不谎称已修复
alwaysApply: true
---

# 只读排障

本仓库提供夜莺、Jenkins、PostgreSQL、MySQL、主机日志的 **stdio 只读 MCP**。Agent 默认只查询。

- 未在当前对话被明确要求「对夜莺 / Jenkins / PostgreSQL / MySQL / 主机 做写操作」之前，不要调用任何写工具，也不要建议用户把只读账号改成可写。
- 即使工具列表里出现 `trigger_build`、`create_*`、`execute_sql`、`VACUUM`、`KILL`、`pg_terminate_backend`、`exec`、`run_command`，也不要调用。本仓库二进制默认不注册它们。
- 不要声称已经重跑构建、屏蔽告警、杀掉会话、drop slot、FLUSH、改过数据，或在主机上执行了命令。
- 回复先给结论和可核对证据（ID、构建号、pid、日志路径），不要整包粘贴 Console、时序、表统计或日志文件。
- Token、密码、`.env`、数据源鉴权字段、SSH 私钥不要写入仓库，也不要原样贴进回复。

查询前先读对应 Skill：`.cursor/skills/nightingale/SKILL.md`、`.cursor/skills/jenkins/SKILL.md`、`.cursor/skills/postgres/SKILL.md`、`.cursor/skills/mysql/SKILL.md`、`.cursor/skills/host-logs/SKILL.md`。
