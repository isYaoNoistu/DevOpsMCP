---
description: 开源仓库脱敏：示例不得包含真实主机、Token、内部业务名
globs: "**/*.md"
alwaysApply: false
---

# 脱敏

文档和样例只允许虚构占位：

- 主机：`example.com`、`db.example.com`、`127.0.0.1`
- Job：`team/prod/checkout-api`
- 数据库 target：`orders-prod`、`shop-prod`、`local-dev`、`mcp-lab-local`
- 日志索引：`app-logs-*`
- 密钥：`<nightingale-personal-token>`、`<jenkins-api-token>` 这类占位符

禁止提交：内网 IP、真实域名、客户/产品线名称、个人路径（如 `C:/Users/...`）、生产 Token、生产密码、`pgpass` 真实行。

允许：`lab/` 目录中明确标明「仅一次性 Docker 实验室」的口令（例如 `mcp-lab-readonly`），不得复制到生产示例或 `examples/`。

改 README 或 example 时同步检查 Skill 里的示例是否仍是虚构值。
