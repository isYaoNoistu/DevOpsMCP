interface:
  display_name: "MySQL 查询"
  short_description: "查询本机清单中的 MySQL：会话、锁、统计、复制、参数与只读 SQL"
  default_prompt: "使用 $mysql 解析 target 后只读排查数据库问题，先专用 Tool，不够再用 query_mysql。"

dependencies:
  tools:
    - type: "mcp"
      value: "mysql"
      description: "Read-only MySQL diagnosis tools"
